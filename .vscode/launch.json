{
  "version": "0.2.0",
  "configurations": [
    {
      "cwd": "${workspaceFolder}/backend",
      "name": "pytest",
      "type": "python",
      "request": "launch",
      "module": "pytest",
      "justMyCode": false,
      "args": ["tests", "--no-cov"] // --no-cov is required for breakpoints to work with pytest
    }
  ]
}
